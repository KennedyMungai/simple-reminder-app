import React from "react";

type Props = {};

const Logo = (props: Props) => {
  return (
    <h1 className="text-2xl font-bold text-white bg-gradient-to-r from-red-500 to-orange-500 bg-clip-text text-transparent">
      SimpleReminder
    </h1>
  );
};

export default Logo;
